#!/bin/sh
set -e
cd "$SRCDIR"
cd "$source_dir" || log_error "In $package_name.build: Failed to cd to $source_dir"
./config --prefix=/usr         \
     --openssldir=/etc/ssl \
     --libdir=lib          \
     shared                \
     zlib-dynamic
