#!/bin/sh
# shellcheck disable=SC2034

package_name="efivar"
package_version="39"
package_description=""
package_source="https://github.com/rhboot/efivar/archive/39/efivar-39.tar.gz d41d8cd98f00b204e9800998ecf8427e"

source_dir="$package_name-$package_version"

configure() {
    cd "$source_dir" || log_error "In $package_name.build: Failed to cd to $source_dir"
    :
}

build() {
    make ENABLE_DOCS=0
}

install_files() {
    make ENABLE_DOCS=0 LIBDIR=/usr/lib install
}
